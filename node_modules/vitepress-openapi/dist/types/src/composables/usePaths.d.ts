import type { OpenAPIDocument } from '../types';
export declare function usePaths({ spec, defaultTag, defaultTagDescription, }: {
    spec: OpenAPIDocument;
    defaultTag?: string;
    defaultTagDescription?: string;
}): {
    getPathsByVerbs: () => any[];
    getTags: () => {
        name: string | null;
        description: string | null;
    }[];
};
