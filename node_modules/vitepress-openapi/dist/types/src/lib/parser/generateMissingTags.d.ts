import type { OpenAPIDocument } from '../../types';
export declare function generateMissingTags({ spec, defaultTag, defaultTagDescription, }: {
    spec: OpenAPIDocument;
    defaultTag?: string;
    defaultTagDescription?: string;
}): OpenAPIDocument;
