import{_ as t,o as a,c as o,az as i,aN as n,aO as s,aP as r}from"./chunks/framework.DXXa9EJF.js";const f=JSON.parse('{"title":"Dynamic Upload Ratio","description":"","frontmatter":{},"headers":[],"relativePath":"guide/dynamic-upload-ratio.md","filePath":"guide/dynamic-upload-ratio.md"}'),l={name:"guide/dynamic-upload-ratio.md"};function h(d,e,p,u,m,c){return a(),o("div",null,[...e[0]||(e[0]=[i('<h1 id="dynamic-upload-ratio" tabindex="-1">Dynamic Upload Ratio <a class="header-anchor" href="#dynamic-upload-ratio" aria-label="Permalink to “Dynamic Upload Ratio”">​</a></h1><p>Normally a template is stuck with one aspect ratio. That&#39;s fine right up until you want to reuse it. Say you&#39;ve built a lovely &quot;Van Gogh&quot; effect and you want it on a tall poster, a square avatar, and a wide banner — with a fixed ratio you&#39;d end up cloning that template three times, once per shape. Tedious, and easy to get out of sync.</p><p><strong>Dynamic Upload Ratio</strong> fixes that. Set a template&#39;s ratio to <strong>Auto</strong>, and it stops caring about shape ahead of time. Instead, it takes its shape from whatever image you feed it. One template, any ratio.</p><h2 id="when-to-use-it" tabindex="-1">When to use it <a class="header-anchor" href="#when-to-use-it" aria-label="Permalink to “When to use it”">​</a></h2><p>Auto is for when the <em>same</em> generation should come back in the shape of whatever went in.</p><p>The clearest example is a reusable Teeinblue template: one ImagenHub effect shared across a whole library of artworks and layers that are all different sizes — portrait here, square there, something wide over there — without building a separate template for each one.</p><p>If you actually <em>want</em> a template to always spit out one fixed shape, don&#39;t use Auto. Leave it on a concrete ratio. Auto is entirely opt-in, and turning it on for one template changes nothing about the rest.</p><h2 id="how-it-works" tabindex="-1">How it works <a class="header-anchor" href="#how-it-works" aria-label="Permalink to “How it works”">​</a></h2><p>For any model whose ratio (or size) comes from a fixed list, that list now has one extra entry at the bottom: <strong>Auto</strong>.</p><p><img src="'+n+'" alt="The Select Size dropdown on a template, with Auto listed below the fixed sizes"></p><p>Choose <strong>Auto</strong> and the shape decision moves out of the template and onto the image you send:</p><ol><li>The image&#39;s own width and height come along with the request.</li><li>ImagenHub looks at those dimensions and picks the <strong>closest ratio the model actually offers</strong>. Orientation always holds — feed it something wide and you get something wide back, never a surprise flip to portrait.</li><li>That resolved ratio is what gets generated.</li></ol><p>A few things that are handy to know:</p><ul><li><strong>It snaps to the nearest option, it doesn&#39;t invent one.</strong> A model only knows the ratios in its list, so an odd size like 1000 × 1010 lands on the closest match (a square, here) rather than throwing an error.</li><li><strong>No dimensions? No problem.</strong> If nothing about size comes through, Auto quietly falls back to the model&#39;s own default ratio and carries on.</li><li><strong>Your fixed-ratio templates are safe.</strong> Anything pinned to a real ratio keeps behaving exactly as it always has.</li></ul><div class="tip custom-block"><p class="custom-block-title">A concrete ratio always beats Auto</p><p>If a template pins a real size instead of Auto, that size wins and the incoming shape is ignored. So if an Auto template keeps handing you squares, the first thing to check is that it&#39;s really set to <strong>Auto</strong> and not a fixed size.</p></div><h2 id="using-it-with-teeinblue" tabindex="-1">Using it with Teeinblue <a class="header-anchor" href="#using-it-with-teeinblue" aria-label="Permalink to “Using it with Teeinblue”">​</a></h2><p>This is really what Auto was built for. A Teeinblue artwork library is full of layers at all sorts of sizes — a 2160 × 3840 portrait, a 2048 × 2048 square, a 1600 × 1600 print — and any of them might lean on the same ImagenHub effect.</p><p><img src="'+s+'" alt="A Teeinblue artwork library showing artworks at different ratios — portrait, square, and wide"></p><p>With Auto, they can all share <strong>one</strong> template — no per-shape duplicates to maintain. The shape each generation comes back in is decided by the layer you set up, not the template.</p><p>The important part is that you still set the ratio yourself, once, on the layer. In the Teeinblue artwork editor, create a <strong>Photo Upload</strong> layer at the ratio you want that artwork to be — a tall layer for a portrait piece, a square one for a square piece. Then open its personalization settings and attach your ImagenHub template under <strong>ImagenHub Template</strong>.</p><p><img src="'+r+'" alt="A Teeinblue layer&#39;s personalization settings with an ImagenHub template attached under Image effects"></p><p>From there it&#39;s automatic. When a customer uploads their own photo into that layer, Auto snaps it to the nearest ratio the model supports for that layer&#39;s shape — so whatever they upload comes out matching the artwork. You build the layer at the right ratio once; every customer&#39;s upload falls into line after that.</p><h2 id="good-to-know" tabindex="-1">Good to know <a class="header-anchor" href="#good-to-know" aria-label="Permalink to “Good to know”">​</a></h2><ul><li><strong>The old meaning of <code>auto</code> is gone.</strong> On a couple of models <code>auto</code> used to mean &quot;let the provider pick.&quot; It now means &quot;match the shape I sent.&quot; Hardly anyone was using the old behaviour, so it was retired rather than kept around to confuse things.</li><li><strong>Raw-dimension models are a different story.</strong> Some models take a free-form width and height with no ratio list at all. There&#39;s no shape option for Auto to hook into on those, so the dimensions you send simply <em>are</em> the output size.</li></ul><h2 id="next-steps" tabindex="-1">Next steps <a class="header-anchor" href="#next-steps" aria-label="Permalink to “Next steps”">​</a></h2><ul><li><a href="./templates">Templates</a> — how reusable presets store their inputs.</li><li><a href="./image-generation">Image Generation</a> — the flow Auto plugs into.</li><li><a href="./../teeinblue/">Teeinblue Integration</a> — connecting ImagenHub templates to Teeinblue layers.</li></ul>',26)])])}const w=t(l,[["render",h]]);export{f as __pageData,w as default};
